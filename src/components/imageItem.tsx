import { Button, Image } from "@heroui/react"

export const ImageItem = () => {
    return (
        <div className="relative flex">
            {/* <Button className="absolute top-0 right-0 z-50 font-bold mr-1 mt-1" variant="faded" isIconOnly></Button>
            <Image
                src={src}
                className="w-full"
            /> */}
        </div>
    )
}
